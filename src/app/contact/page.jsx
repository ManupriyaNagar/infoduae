import ContactPage from '@/components/Contact/ContactPage';
import React from 'react';

export const metadata = {
  title: 'Contact | AlgoTrade',
  description: 'Contact us for any queries or feedback.',
};

const Contact = () => {
    return (
        <div>
           <ContactPage/>
        </div>
    );
};

export default Contact;