/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  // If deploying to a subdirectory, uncomment and adjust the basePath
  // basePath: '/your-subdirectory',
  // If you have a custom assetPrefix, uncomment and adjust
  // assetPrefix: '/your-subdirectory',
};

export default nextConfig;