/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    // ... other paths
  ],
  theme: {
    extend: {
      colors: {
        'custom-teal': '#00FFAA', // A bright teal/cyan color similar to the image
        // You can adjust the hex code to precisely match the image's color
      },
    },
  },
  plugins: [],
}